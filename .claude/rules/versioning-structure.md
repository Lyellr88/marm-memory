# Versioning Structure

**Versioning follows [Semantic Versioning](https://semver.org/):**

- **MAJOR** — breaking changes (JSON schema renames, parameter removals, anything that breaks existing consumers)
- **MINOR** — new scripts, new parameters, new features
- **PATCH** — bug fixes, test fixes, comment/doc updates