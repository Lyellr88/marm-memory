# Commit Workflow Rules

**Never auto-commit code changes**  
**Wait for explicit user request** (`/commit`, "ok commit this", etc.)

Commits bypass user review. User reviews all changes before committing.
