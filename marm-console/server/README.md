# Console Server

The standalone MARM Console API host runs on `127.0.0.1:8002` by default.

```powershell
cd marm-console
python -m server
```

`MARM_DB_PATH` can point the Console at a non-default local MARM memory database.
